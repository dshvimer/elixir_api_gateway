<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :path %>
  <%= text_input f, :path %>
  <%= error_tag f, :path %>

  <%= label f, :upstream %>
  <%= text_input f, :upstream %>
  <%= error_tag f, :upstream %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
